import { useState } from "react";
import type { NativeStackScreenProps } from "@react-navigation/native-stack";
import { Text, TextInput } from "react-native";
import type { CustomerStackParamList } from "../../../app/navigation/navigation.types";
import { Button, Card, ScreenContainer, SuccessState } from "../../../shared/components";
import { reviewRepository } from "../../../demo/adapters/demoReviewRepository";
export function CustomerRatingScreen({ route, navigation }: NativeStackScreenProps<CustomerStackParamList, "CustomerRating">) { const [rating, setRating] = useState("5"); const [comment, setComment] = useState(""); const [done, setDone] = useState(false); if (done) return <ScreenContainer><SuccessState message="شكراً لتقييمك. أضفنا نقاط المكافأة إلى حسابك." /><Button onPress={() => navigation.popTo("CustomerTabs")}>فتح المكافآت</Button></ScreenContainer>; return <ScreenContainer><Text style={styles.title}>قيّم تجربتك مع الفني</Text><Card><Text style={styles.text}>التقييم العام من 1 إلى 5</Text><TextInput value={rating} onChangeText={setRating} keyboardType="number-pad" style={styles.input} /><Text style={styles.text}>ملاحظتك (اختياري)</Text><TextInput value={comment} onChangeText={setComment} multiline style={[styles.input, { height: 90 }]} /></Card><Button onPress={async () => { await reviewRepository.submit({ jobId: route.params.jobId, technicianId: route.params.technicianId, overall: Number(rating), quality: Number(rating), speed: Number(rating), commitment: Number(rating), communication: Number(rating), comment }); setDone(true); }}>إرسال التقييم</Button></ScreenContainer>; }
const styles = { title: { fontSize: 22, fontWeight: "700" as const, textAlign: "right" as const, writingDirection: "rtl" as const }, text: { textAlign: "right" as const, writingDirection: "rtl" as const, marginVertical: 8 }, input: { borderWidth: 1, borderColor: "#ddd", borderRadius: 10, padding: 12, textAlign: "right" as const, writingDirection: "rtl" as const } };
