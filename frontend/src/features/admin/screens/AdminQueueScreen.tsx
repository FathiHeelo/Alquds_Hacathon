import { Text } from "react-native";
import { Button, Card, ScreenContainer } from "../../../shared/components";
export function AdminQueueScreen({ kind }: { kind: "verification" | "reports" | "risk" }) { const data = kind === "verification" ? ["طارق المقدسي", "رنا الحسيني"] : kind === "reports" ? ["بلاغ: تأخر الفني عن الموعد", "بلاغ: وصف غير دقيق للطلب"] : ["مؤشر متوسط المخاطر · يحتاج مراجعة بشرية", "إشارات: تكرار إلغاء الطلبات"]; return <ScreenContainer><Text style={styles.title}>{kind === "verification" ? "طابور توثيق الفنيين" : kind === "reports" ? "البلاغات والسلامة" : "مراجعة مخاطر AI"}</Text>{data.map((item) => <Card key={item}><Text style={styles.text}>{item}</Text><Button variant="outlined" onPress={() => undefined}>{kind === "reports" ? "فتح البلاغ" : "مراجعة بشرية"}</Button></Card>)}</ScreenContainer>; }
const styles = { title: { fontSize: 22, fontWeight: "700" as const, textAlign: "right" as const, writingDirection: "rtl" as const }, text: { fontSize: 16, textAlign: "right" as const, writingDirection: "rtl" as const, marginBottom: 10 } };
