import { useState } from "react";
import { Text, TextInput } from "react-native";
import type { NativeStackScreenProps } from "@react-navigation/native-stack";
import type { TechnicianStackParamList } from "../../../app/navigation/navigation.types";
import { Button, Card, ScreenContainer, SuccessState } from "../../../shared/components";
export function TechnicianOfferScreen({ navigation, route }: NativeStackScreenProps<TechnicianStackParamList, "TechnicianCreateOffer">) { const [price, setPrice] = useState(String(route.params.suggestedPrice ?? 125)); const [message, setMessage] = useState(route.params.suggestedMessage ?? "أصل خلال 20 دقيقة ومعي القطعة المناسبة."); const [sent, setSent] = useState(false); if (sent) return <ScreenContainer><SuccessState message="تم إرسال العرض للعميل" /><Button onPress={() => navigation.popTo("TechnicianTabs")}>العودة للوحة الفني</Button></ScreenContainer>; return <ScreenContainer><Text style={styles.title}>عرض صيانة جديد</Text><Card><Text style={styles.text}>السعر (₪)</Text><TextInput value={price} onChangeText={setPrice} keyboardType="number-pad" style={styles.input} /><Text style={styles.text}>الوصول: 20 دقيقة · مدة العمل: 40 دقيقة</Text><TextInput value={message} onChangeText={setMessage} style={[styles.input, { height: 80 }]} multiline /></Card><Button onPress={() => setSent(true)}>إرسال العرض</Button></ScreenContainer>; }
const styles = { title: { fontSize: 22, fontWeight: "700" as const, textAlign: "right" as const, writingDirection: "rtl" as const }, text: { textAlign: "right" as const, writingDirection: "rtl" as const, marginVertical: 8 }, input: { borderWidth: 1, borderColor: "#ddd", borderRadius: 10, padding: 12, textAlign: "right" as const } };
